version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "daily"
    versioning-strategy: increase-if-necessary
    ignore:
      # Renovate (also enabled) handles major bumps deliberately; Dependabot
      # was opening unmergeable majors (TS 7, eslint 10 not yet supported).
      - dependency-name: "*"
        update-types: ["version-update:semver-major"]
    groups:
      nextjs:
        patterns: ["next", "eslint-config-next"]
      react:
        patterns: ["react", "react-dom", "@types/react", "@types/react-dom"]
      testing:
        patterns:
          - "vitest"
          - "@vitest/*"
          - "@playwright/test"
          - "@testing-library/*"
          - "jsdom"
          - "@stryker-mutator/*"
      tooling:
        patterns:
          - "eslint"
          - "prettier"
          - "prettier-plugin-*"
          - "typescript"
          - "husky"
          - "lint-staged"
          - "tailwindcss"
          - "@tailwindcss/*"
    open-pull-requests-limit: 5

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "daily"
    groups:
      actions:
        patterns: ["*"]
